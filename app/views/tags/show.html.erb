<h1>Tasks Tagged with <%= @tag.name %></h1>
<%= link_to "Main_page", controller: 'tasks' %>
<ul>
  <% @tag.tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

