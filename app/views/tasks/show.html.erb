<p>
  <strong>Time:</strong>
  <%= @task.title %>
</p>
 
<p>
  <strong>Action:</strong>
  <%= @task.text %>
</p>

<p>
  Tags:
  <% @task.tags.each do |tag| %>
    <%= link_to tag.name, tag_path(tag) %>
  <% end %>
</p>
<%= link_to 'Back', tasks_path, class: 'btn btn-default btn-xs' %> |
<%= link_to 'Mark', complete_task_path(@task), class: 'btn btn-primary btn-xs' %> |
